# Configuración del servidor
PORT=5001
NODE_ENV=development

# MongoDB Atlas
# Reemplaza con tu propia cadena de conexión de MongoDB Atlas
MONGODB_URI=mongodb+srv://<usuario>:<password>@<cluster>.mongodb.net/desvare?retryWrites=true&w=majority&appName=desvare-new

# JWT Secret
# Cambia esto por un secreto seguro en producción
JWT_SECRET=tu_secret_super_seguro_aqui_cambiar_en_produccion

# URLs permitidas (CORS)
CLIENT_URL=http://localhost:5173
DRIVER_URL=http://localhost:5174
